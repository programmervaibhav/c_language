#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter The vaule of a:");
    scanf("%d", &a);
    printf("Enter The vaule of b:");
    scanf("%d", &b);
    printf("The Sum of a and b is:%d", a + b);
    return 0;
}