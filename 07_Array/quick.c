#include<stdio.h>
void display(int row, int col){
for (int i = 0; i < row; i++)
{
    for (int j = 0; j < col; j++)
    {
        printf("Enter the number of row and col");
        
    }
    
}

}
int main(){
int row,col;
array[row][col];

 printf("Enter the number of row and col");
        scanf("%d %d",&row,&col);
return 0;
}