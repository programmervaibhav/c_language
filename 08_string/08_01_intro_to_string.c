#include <stdio.h>
//\0 --> idicate the end of string/here the strinng is terminate
int main()
{
    // There are two different types of intializing the strings
    // 1.
    //  char str[]={'v','a','i','b','h','a','v' \0};

    // 2. shorcut
    char str[] = "VAIBHAV";//here we not required to include\0 i.e. null

    return 0;
}